#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    int n, soma = 0, i;
    

    printf("Quantas alunos você vai digitar?\n");
    scanf("%d", &n);
    
    int media[n];
    int nota_1[n];
    int nota_2[n];
    char nome_alunos[n][50];

    for (i = 0; i < n; i++) {
        printf("Digite o nome do aluno: ");
        scanf("%s", nome_alunos[i]);
        
        printf("Digite a nota do 1 semestre do aluno: ");
        scanf("%d", &nota_1[i]);
        printf("Digite a nota do 2 semestre do aluno: ");
        scanf("%d", &nota_2[i]);
 
        media[i] = (nota_1[i] + nota_2[i]) / 2;
        
        if(media[i] >= 6){
            printf("Alunos aprovados: %s, nota: %d\n", nome_alunos[i], media[i]);
        }
        
    }
        
    return 0;
}
